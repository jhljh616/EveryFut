server:
  port : 8081

spring:
  datasource:
    username: jihu
    password: 1234
    mapper-locations: classpath:/mappers/**/*.xml
    jdbc-url: jdbc:mariadb://localhost:3306/futcord
    driver: org.mariadb.jdbc.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  config-location: classpath:/mybatis/mybatis-config.xml